class Solution:
    def isPanagram(self,s):
        #your code here
        res=set()
        s=s.upper()
        for c in s:
            if c.isalpha():
                res.add(c)
        if len(res)==26:
            return 1
        else:
            return 0
